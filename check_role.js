const mongoose = require(" mongoose\); require(\dotenv\).config(); async function run() { try { await mongoose.connect(process.env.MONGODB_URI); const User = mongoose.model(\UserRoleCheck\, new mongoose.Schema({ email: String, role: String }, { collection: \users\ })); const users = await User.find({ role: \admin\ }); console.log(\Result:\); users.forEach(u => console.log(JSON.stringify({ email: u.email, role: u.role }))); } catch (e) { console.log(\Error:\, e.message); } finally { await mongoose.disconnect(); } } run();
